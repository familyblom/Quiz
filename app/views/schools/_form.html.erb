<%= form_for(@school) do |f| %>
  <% if @school.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@school.errors.count, "error") %> prohibited this school from being saved:</h2>

      <ul>
      <% @school.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :left_operand %><br />
    <%= f.number_field :left_operand, :type => 'float'%>
  </div>
  <div class="field">
    <%= f.label :operator %><br />
    <%= f.select :operator, options_for_select([["+", "+"], ["-", "-"], ["x", "*"], ["/", "/"]]) %>
  </div>
  <div class="field">
    <%= f.label :right_operand %><br />
    <%= f.number_field :right_operand, :type => 'float' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
